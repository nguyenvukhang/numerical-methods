#![allow(non_snake_case)] // matrices are traditionally written in upper case.

#[macro_use]
mod assert;
mod interpolation;
mod matrix;
mod na;
mod prelude;

use prelude::*;

fn main() -> Result<()> {
    Ok(())
}
